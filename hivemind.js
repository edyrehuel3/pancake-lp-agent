import { log } from "./logger.js";

export async function hivemindSync() {
  log("hivemind", "HiveMind sync placeholder - not yet configured");
  return { synced: false, reason: "not_configured" };
}
